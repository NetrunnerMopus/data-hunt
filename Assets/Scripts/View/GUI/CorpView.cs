namespace view.gui
{
    public class CorpView
    {
        public readonly ServerRow serverRow;

        public CorpView(ServerRow serverRow)
        {
            this.serverRow = serverRow;
        }
    }
}